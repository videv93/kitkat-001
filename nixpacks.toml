# Railway/Nixpacks configuration for Kitkat
# Nixpacks auto-detects Python from runtime.txt and installs requirements.txt

[variables]
PYTHONPATH = "/app/src"

[start]
cmd = "uvicorn kitkat.main:app --host 0.0.0.0 --port ${PORT:-8000}"
