# Auth
WEBHOOK_TOKEN=your-128-bit-secret-token-here

# Telegram Alerts
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# DEX Credentials (Extended)
EXTENDED_API_KEY=
EXTENDED_API_SECRET=

# Feature Flags & Test Mode Configuration (Story 3.1)
# Set to 'true' for test mode (restart required), 'false' for production
# WARNING: Changing TEST_MODE requires application restart
# - TEST_MODE=true: Uses MockAdapter, no real trades executed, safe for testing
# - TEST_MODE=false: Uses real DEX adapters (production mode - default)
TEST_MODE=false

# Database (optional - defaults to project_root/kitkat.db)
# DATABASE_URL=sqlite+aiosqlite:///./kitkat.db
